/**
 * ServerCloser
 */
class ServerCloser extends require('events').EventEmitter {
  /**
   * ServerCloser close
   * @singleton
   */
  constructor() {
    super();
  }
}

module.exports = ServerCloser;
